{
  "name": "csv-parsing-server",
  "version": "1.0.0",
  "description": "This is a parser that decode CSV file, conver it into an Object, and do an action on the data using GraphQl API",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server"
  },
  "author": "Anasite",
  "license": "MIT",
  "dependencies": {
    "@shopify/shopify-api": "^6.0.2",
    "color": "^4.2.3",
    "csv": "^6.2.5",
    "express": "^4.18.2",
    "node-fetch": "^3.3.0",
    "sqlite3": "^5.1.4"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
